fragment UserFields on auth_users {
  id
  first_name
  last_name
  email
}

query User {
  auth_users {
    ...UserFields
  }
}

query AllUsers {
  auth_users {
    email
    encrypted_password
    first_name
    id
    last_name
  }
}
